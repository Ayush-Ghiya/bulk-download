{
  "name": "bulk-download-demo",
  "private": true,
  "type": "module",
  "workspaces": ["server", "web"],
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/archiver": "^6.0.2"
  },
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "cd web && bun run build",
    "typecheck": "bun run --filter '*' typecheck",
    "preview": "bun run --filter bulk-download-demo-web preview",
    "start:server": "bun run --filter bulk-download-demo-server start"
  }
}
